# 🚀 كيفية تشغيل مشروع Code Maestro

## المتطلبات الأساسية

تأكد من تثبيت البرامج التالية على جهازك:

### 1️⃣ تثبيت Rust
```bash
# Windows: اذهب إلى
https://rustup.rs/
# اتبع التعليمات على الموقع

# macOS / Linux:
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### 2️⃣ تثبيت Node.js
```bash
# اذهب إلى:
https://nodejs.org/
# ثم ثبت الإصدار LTS
```

### 3️⃣ التحقق من التثبيت
```bash
rustc --version
cargo --version
node --version
npm --version
```

---

## الخطوة 1️⃣: التحضير (5 دقائق)

```bash
# اذهب إلى مجلد المشروع
cd d:\Pro\mm

# انسخ ملف البيئة
cp .env.example .env

# ثبت الحزم
cargo build
npm install
```

---

## الخطوة 2️⃣: تشغيل المشروع (اختر واحد)

### الخيار A: تشغيل البرنامج (الطريقة البسيطة)

افتح 3 نوافذ Command Prompt / PowerShell:

**نافذة 1 - Backend (الخادم الخلفي):**
```bash
cd d:\Pro\mm
cargo run
```

**نافذة 2 - Frontend (الواجهة الأمامية):**
```bash
cd d:\Pro\mm
npm run dev
```

**نافذة 3 - تطبيق سطح المكتب:**
```bash
cd d:\Pro\mm
cargo tauri dev
```

### الخيار B: تشغيل الاختبارات

```bash
# اختبار Backend
cargo test

# اختبار Frontend
npm run test

# اختبار الكل مع التفاصيل
cargo test -- --nocapture
```

### الخيار C: البناء للإصدار (Release)

```bash
# بناء Backend
cargo build --release

# بناء Frontend
npm run build

# بناء تطبيق سطح المكتب
cargo tauri build
```

---

## 📍 بعد التشغيل - ماذا تتوقع؟

### Backend (Cargo run)
```
✓ Starting Code Maestro...
✓ Database initialized
✓ LSP server started
✓ AI engine initialized
✓ Code Maestro is ready!
```

### Frontend (npm run dev)
```
✓ Vite development server
✓ VITE v5.0.0  running at:
✓ ➜  Local:   http://localhost:5173/
```

### Desktop App (cargo tauri dev)
```
✓ Tauri application starting
✓ WebView loading
✓ Application ready
```

---

## 🌐 الوصول إلى التطبيق

بعد تشغيل `npm run dev`:

**افتح المتصفح وذهب إلى:**
```
http://localhost:5173
```

أو انتظر تطبيق سطح المكتب من `cargo tauri dev`

---

## ⚡ الأوامر السريعة

### تشغيل البرنامج
```bash
cargo run              # تشغيل Backend
npm run dev           # تشغيل Frontend
cargo tauri dev       # تطبيق سطح المكتب
```

### الاختبار
```bash
cargo test            # اختبار Rust
npm run test          # اختبار React
cargo test --lib     # اختبار المكتبة فقط
```

### الترتيب والتنظيف
```bash
cargo fmt             # ترتيب كود Rust
cargo clippy          # فحص الأخطاء
cargo clean           # حذف ملفات البناء
```

### بناء الإصدار
```bash
cargo build --release     # بناء Rust optimized
npm run build            # بناء React
cargo tauri build        # بناء التطبيق الكامل
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "Port already in use"
```bash
# تغيير المنفذ في vite.config.js
# غيّر 5173 إلى 5174
```

### المشكلة: "Rust not found"
```bash
rustup update          # تحديث Rust
rustc --version        # تحقق من الإصدار
```

### المشكلة: "npm packages missing"
```bash
rm -rf node_modules    # احذف المجلد
npm install            # أعد التثبيت
```

### المشكلة: "Database errors"
```bash
rm data/maestro.db     # احذف قاعدة البيانات
# سيتم إنشاء واحدة جديدة تلقائياً
```

---

## 📊 هيكل المشروع

```
Code Maestro/
├── src/                    # كود Rust
│   ├── *.rs               # الملفات الرئيسية
│   └── integration_tests.rs
├── Frontend Files/
│   ├── *.jsx              # مكونات React
│   ├── *.css              # أنماط CSS
│   └── api.js             # عميل API
├── Configuration/
│   ├── Cargo.toml
│   ├── package.json
│   ├── vite.config.js
│   └── tauri.conf.json
└── Documentation/
    ├── README.md
    ├── QUICK_REFERENCE.md
    └── [ملفات أخرى]
```

---

## 📚 الملفات المهمة

### للبدء
- `00_START_HERE.md` - ابدأ من هنا
- `QUICK_REFERENCE.md` - أوامر سريعة
- `INSTALLATION.md` - تعليمات التثبيت

### للتطوير
- `ARCHITECTURE.md` - معمارية النظام
- `CONTRIBUTING.md` - إرشادات التطوير
- `PROGRESS_REPORT.md` - الحالة الحالية

---

## ✅ قائمة التحقق

- [ ] تثبيت Rust
- [ ] تثبيت Node.js
- [ ] نسخ `.env.example` إلى `.env`
- [ ] تشغيل `cargo build`
- [ ] تشغيل `npm install`
- [ ] فتح 3 نوافذ Command
- [ ] تشغيل Backend: `cargo run`
- [ ] تشغيل Frontend: `npm run dev`
- [ ] تشغيل Desktop: `cargo tauri dev`
- [ ] الدخول إلى http://localhost:5173
- [ ] اختبار الميزات

---

## 💡 نصائح مهمة

1. **اترك كل نافذة مفتوحة** - لا تغلق نوافذ الأوامر
2. **الانتظار قليلاً** - البناء الأول يستغرق وقتاً
3. **تحقق من المنافذ** - استخدم منافذ مختلفة إذا كانت مشغولة
4. **اقرأ الأخطاء** - الأخطاء واضحة وتساعد في الحل
5. **استخدم DevTools** - F12 في المتصفح للتصحيح

---

## 🎯 الخطوات التالية بعد التشغيل

1. **استكشف الواجهة**
   - اضغط على أيقونة جديد ملف
   - افتح ملف
   - اختبر Terminal

2. **جرب الميزات**
   - اكتب كود
   - لاحظ الاقتراحات
   - اختبر حفظ الملفات

3. **اقرأ التوثيق**
   - اقرأ ARCHITECTURE.md
   - ادرس الملفات
   - افهم البنية

4. **ابدأ التطوير**
   - اختر مهمة من PROGRESS_REPORT.md
   - ابدأ في التطوير
   - أضف ميزات جديدة

---

## 📞 تحتاج إلى مساعدة؟

**اقرأ هذه الملفات:**
1. `QUICK_REFERENCE.md` - أوامر سريعة
2. `INSTALLATION.md` - تعليمات التثبيت
3. `CONTRIBUTING.md` - إرشادات التطوير
4. `ARCHITECTURE.md` - فهم النظام

---

## 🚀 كل شيء جاهز!

**الآن أنت مستعد للبدء:**

```bash
# 1. التحضير
cd d:\Pro\mm
cp .env.example .env
cargo build
npm install

# 2. التشغيل (3 نوافذ)
cargo run           # نافذة 1
npm run dev         # نافذة 2
cargo tauri dev     # نافذة 3

# 3. الاستمتاع!
# اذهب إلى http://localhost:5173
```

---

**استمتع ببرمجتك! 💻✨**
